# Budget App 项目交接记忆

更新日期：2026-08-06

## 项目与目标

- GitHub：`ZORA-MAX/budget-app`
- 技术栈：Vite、React 18、Tailwind CSS、IndexedDB
- 用途：导入微信、支付宝和记账本账单，自动分类并生成月度消费分析。
- 本次新增：银行卡没有 CSV/Excel 时，可上传交易明细截图并新增账单。

## 本次实现

- 增加 GitHub Pages 稳定备用入口，避免部分网络环境对 `vercel.app` 域名的异常解析。
- “历史记录”页增加版本化完整 JSON 备份和恢复：保留全部交易字段及人工分类；恢复时覆盖同月、保留其他本机月份。
- 修复历史页手动新增、删除交易后只更新界面但未写回 IndexedDB 的问题。
- 数据库版本提升到 3，兼容已被旧版本网页升级过的浏览器数据，避免历史页因 IndexedDB `VersionError` 无法打开。
- 从历史月份手动新增账单时，日期默认落在正在查看的月份，避免账单被写入错误月份。
- 删除整月时同步删除该月人工分类，避免遗留孤立数据。
- GitHub Pages 是纯静态入口，AI 深度分析仍需 Vercel 后端；本地月度小结不受影响。

- 新增银行卡截图 OCR 导入区，支持 PNG、JPG、WebP、HEIC/HEIF。
- 支持一次选择多张截图，并按顺序逐张识别，避免手机同时运行多个 OCR 任务。
- OCR 使用 `tesseract.js`，仅在用户开始识别后动态加载；截图在浏览器本地处理。
- 自动提取日期、商户、金额；识别结果在保存前可修改、删除或补录。
- 负数扣款记为支出；正数退款、入账和冲正默认排除。
- 针对中国银行交易明细截图：
  - 从 `YYYY.MM` 与左侧日号组合日期。
  - 截图顶部日号被滚动隐藏时，根据下一个日号推断前一天。
  - 忽略“余额”数值，只读取行尾扣款金额。
  - 修正常见 OCR 错字，如“拌音/择音支付”→“抖音支付”、“叮噬买荣”→“叮咚买菜”。
- 无截图时，仍可在分析报告的“逐笔明细”中手动新增账单。

## 已验证样本

- 使用 4 张中国银行 2026 年 7 月交易明细截图完成真实浏览器测试。
- 截图共显示 32 条流水：28 笔绿色负数扣款、4 笔红色退款/入账。
- 识别结果：28 笔有效支出，金额合计 `¥841.41`，日期范围 `2026-07-02` 至 `2026-07-19`。
- 4 笔正数记录被正确排除；余额没有被误记为消费。
- 正式构建 `npm run build` 通过。
- 浏览器仅有 React Router v7 future-flag 提示，无本功能运行错误。

## 使用方式

1. 在“导入分析”页选择一张或多张银行卡账单截图。
2. 点击“开始识别”；首次使用需要联网下载中文 OCR 模型。
3. 核对每笔日期、商户和金额，必要时修改或删除。
4. 点击“确认加入”，再点击“生成分析报告”。

## 注意事项与后续建议

- OCR 结果必须人工复核，尤其是被银行界面截断的商户名称。
- 银行转账的负数流水会被识别为支出。例如 `互联互通 ZHANG MENGXIN -39.00` 如果是本人账户间转账，应删除或归类为“资金流转/非消费”。
- 图片和账单数据默认保存在本地浏览器；不要把真实账单截图提交到 GitHub。
- `chi_sim.traineddata`、`eng.traineddata` 是本地 OCR 缓存，不属于仓库内容。
- 后续可增加更多银行的专用解析规则、重复截图重叠检测，以及更明确的“收入/退款/转账”审核开关。

## 主要文件

- `src/components/ScreenshotImporter.jsx`：多图选择、OCR 进度、人工核对和导入。
- `src/lib/screenshot-parser.js`：通用截图与中国银行流水解析规则。
- `src/pages/Home.jsx`：截图账单接入现有分析流程。
- `package.json` / `package-lock.json`：加入 `tesseract.js`。
